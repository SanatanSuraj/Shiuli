/**
 * A fixed film-grain wash over the whole page — the "paper" of the letter.
 * Pure CSS + inline SVG noise, zero runtime cost.
 */
export default function Grain() {
  return <div className="grain" aria-hidden="true" />
}
